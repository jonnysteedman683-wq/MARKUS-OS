# .hermes.md — Workspace Project Rules (AEGIS / PHOENIX Architecture)

Hermes Agent: Follow these workspace rules across all tasks in this repository tree.

## Invariants & Mandatory Standards
- **PRIME-DIRECTIVE Compliance:** Execute Directive 0 on every prompt (validate Obsidian & Supermemory state).
- **Execution Over Narration:** Write, execute, and verify code directly. Return concrete tool outputs and working artifacts.
- **Strict Verification Gate:**
  - Python: Run `python -m py_compile <file>` and test execution before declaring done.
  - TypeScript / JavaScript: Run `node --check <file>` or `tsc --noEmit`.
  - PHOENIX Engine: Use `python phoenix_cli.py evolve <file> --optimize` for AST-sandboxed verification.

## Evolutionary Duty
- Every turn must conclude with the 3-loop evolutionary telemetry:
  1. Output Evaluation (Factuality, Execution, Efficiency).
  2. Code Development / Optimization delta.
  3. Skill Curation mutation via `skill_manage`.
- Include targeted inquiries, alternatives, and upgrade trajectories at turn boundaries.
